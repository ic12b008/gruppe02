/*
Die Klasse soll das Interface LogicIf implementieren und muss daher die 
Implementierung der Methode calculateResult() enthalten. Die Methode soll 
folgende Funktionalität haben:
•der erste Parameter ist ein String der den ersten Wert der Berechnung enthält
•der zweite Parameter ist ein String der den zweiten Wert der Berechnung enthält
•der dritte Parameter ist vom Typ Mode (Enumeration siehe Punkt 3.4) der die 
Art der Berechnung angibt
•die String Values werden mit der Methode getNumber() der Klasse Util in einen 
BigDecimal
umgewandelt (ist die Zahl ungültig, wird eine InvalidNumberException ausgelöst)
•je nach Berechnungsmodus wird die gewünschte Berechnung durchgeführt, wird kein
gültiger Modus gefunden wird eine InvalidModeException gethrowt
•verwenden Sie zur Berechnung die Klasse BigDecimal, damit das Ergebnis korrekt
gerundet wird (Konstante aus Interface verwenden)
•die Division durch 0 löst eine ArithmeticException aus, die in einem catch 
Block behandelt werden muss
 */

package at.technikum.bicss.ode.sb09.gruppe02;

/**
 *
 * @author mguasch
 */
public class LogicImpl {
    
}
